"use client";
import React from "react";

interface WelcomeNightProps {
  tripName: string;
  date: string;
  location: string;
  hotelName: string;
  details: string[];
  dinnerDetails: string;
  schedule: { time: string; event: string }[];
}

export default function WelcomeNight({
  tripName,
  date,
  location,
  hotelName,
  details,
  dinnerDetails,
  schedule,
}: WelcomeNightProps) {
  return (
    <section className="mt-20 bg-gradient-to-b from-[#111] to-[#1a1a1a] text-white py-16 px-6 rounded-2xl shadow-lg border border-pink-500/30">
      <div className="max-w-4xl mx-auto text-center">
        <h2 className="text-3xl md:text-4xl font-bold mb-4 text-pink-400">
          GroovinFly Welcome Night — {location}
        </h2>
        <p className="text-lg mb-8 italic text-gray-300">
          Included with your {tripName} package
        </p>

        <div className="bg-[#222] p-6 rounded-xl mb-8 border border-pink-500/20">
          <h3 className="text-2xl font-semibold mb-2">{hotelName}</h3>
          <p className="text-sm text-gray-400 mb-4">{date}</p>
          <ul className="list-disc list-inside space-y-2 text-left mx-auto max-w-md text-gray-200">
            {details.map((item, index) => (
              <li key={index}>{item}</li>
            ))}
          </ul>
        </div>

        <div className="bg-[#1f1f1f] p-6 rounded-xl border border-pink-500/20 mb-8">
          <h3 className="text-2xl font-semibold mb-3 text-pink-400">
            🍽️ Welcome Dinner
          </h3>
          <p className="text-gray-200">{dinnerDetails}</p>
        </div>

        <h3 className="text-2xl font-semibold mb-4 text-pink-400">
          🕓 Schedule
        </h3>
        <div className="grid grid-cols-1 sm:grid-cols-2 gap-4 text-left max-w-2xl mx-auto">
          {schedule.map((item, index) => (
            <div
              key={index}
              className="bg-[#2b2b2b] p-4 rounded-lg border border-pink-500/10"
            >
              <p className="font-bold text-pink-300">{item.time}</p>
              <p className="text-gray-200">{item.event}</p>
            </div>
          ))}
        </div>

        <p className="mt-8 text-gray-400 italic">
          “This night sets the tone for the week — come ready to meet new
          friends, toast to adventure, and ease into the Adriatic rhythm.” 🌅
        </p>
      </div>
    </section>
  );
}